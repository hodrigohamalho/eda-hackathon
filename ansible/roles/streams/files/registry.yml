apiVersion: registry.apicur.io/v1
kind: ApicurioRegistry
metadata:
  name: service-registry
  namespace: hackathon
spec:
  configuration:
    kafkasql:
      bootstrapServers: 'my-source-cluster-kafka-bootstrap.hackathon.svc:9092'
    persistence: kafkasql
